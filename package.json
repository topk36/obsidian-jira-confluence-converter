{
  "name": "obsidian-jira-confluence-converter",
  "version": "0.1.1",
  "description": "Obsidian plugin: copy Markdown as Jira Wiki Markup or Confluence Storage XHTML.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "test": "esbuild test/converters.test.ts --bundle --platform=node --format=esm --target=node18 --outfile=.tmp/converters.test.mjs && node .tmp/converters.test.mjs",
    "validate": "npm test && npm run build && node scripts/validate-plugin.mjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "jira",
    "confluence",
    "markdown",
    "atlassian"
  ],
  "author": "Ildar Kamalov",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.10",
    "esbuild": "^0.25.6",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.3"
  }
}
